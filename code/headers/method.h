#ifndef FUNCTION_H
#define FUNCTION_H

void add (); 		// adds to list

void sub (); 		// remove from list

void menu();		// show the menu

void clear();		// clears the secreen

void create();		// creates new inventory

void update();		// update quantities

void search();		// search for query

void sort (); 		// sorts data in descending order

char* cat(char str[]); // concates space separated string with '_'

#endif
